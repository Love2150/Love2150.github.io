name: Update cyber tips

on:
  schedule:
    # run every 2 hours (adjust if you like)
    - cron: "0 */2 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-tips:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ✅ Always start from the exact state of origin/main
      - name: Sync with remote main
        run: |
          git fetch origin main
          git checkout main
          git reset --hard origin/main

      # 🔧 Your script that updates assets/tips.json
      - name: Generate latest tips.json
        run: |
          # TODO: replace with your real command
          # example: python scripts/update_tips.py
          echo "updating tips.json…" > /dev/null

      # 💾 Auto-commit just tips.json back to main
      - name: Commit updated tips.json
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          branch: main
          file_pattern: assets/tips.json
          commit_message: "chore: update tips.json"

